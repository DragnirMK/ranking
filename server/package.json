{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "node tests/testDb.js",
    "build": "rm -rf build/ && cd ../client && rm -rf build/ && npm run build && cp -R build ../server"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bcrypt": "^5.1.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mongoose": "^7.0.3",
    "shortid": "^2.2.16",
    "socket.io": "^4.6.1",
    "socket.io-client": "^4.6.1"
  }
}
